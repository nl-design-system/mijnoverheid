import { Meta, Story, Canvas, ArgsTable, Description, Source } from "@storybook/addon-docs/blocks";
import { sanitize } from "dompurify"; // Helps with preventing injections via argstable
import { MDXEmbedProvider } from "mdx-embed";
import dedent from "ts-dedent";

<!-- Import component and component styles -->

import "./index.css";

<!-- Import Docs -->

import README from "./README.md";
import contentGuidelines from "./docs/content-guidelines.md";
import accessibilityGuidelines from "./docs/accessibility-guidelines.md";

export const Template = ({ href, label }) =>
  `<nav aria-labelledby="tab-list-title">
    <span id="tab-list-title" class="sr-only">
      Pagina navigatie
    </span>
    <ul class="lo-tab-list" role="menubar">
      <li
        class="lo-tab-list__item"
        role="menuitem"
      >
        <a
          href="${href}"
          class="lo-tab-list__link lo-tab-list__link--active"
          title="${label}"
          aria-current="page"
        >
          <span class="lo-tab-list__text">${label}</span>
          <span class="sr-only">
            Huidige pagina
          </span>
        </a>
      </li>
      <li
        class="lo-tab-list__item"
        role="menuitem"
      >
        <a
          href="${href}"
          class="lo-tab-list__link"
          title="${label}"
        >
          <span class="lo-tab-list__text">${label}</span>
        </a>
      </li>
    </ul>
  </nav>`;

<Meta
  title="Components/TabList"
  argTypes={{
    href: {
      description: "Set the href attribute of the tab item",
      control: "text",
    },
    label: {
      description: "Set the content of the tab item",
      control: "text",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => Template(args),
    },
    status: "IN DEVELOPMENT",
    notes: { UX: README, Content: contentGuidelines, Accessibility: accessibilityGuidelines },
  }}
/>

# Demo Implemented Link Component

To use the component as a web component, make sure the component is defined by including the components `element.js` file

<Canvas>
  <Story name="TabList" args={{ href: "#", label: "Demo tab item" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="TabList" />

## Design Kit

<MDXEmbedProvider>
  <Figma title="TabList" url="file/x" />
</MDXEmbedProvider>
